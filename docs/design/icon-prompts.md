# Skip 图标生成提示

当前项目已接入多套图标切换机制，并提供可编译的自适应图标占位资源。以下提示用于生成 1024x1024 原始图、前景图层和背景图层。所有方案的系统显示名称都必须保持为 `Skip`。

## 通用约束

- Use case: logo-brand
- Asset type: Android adaptive app icon
- Text (verbatim): "Skip"
- Subject: the word "Skip" as the only core visual element, stylized as a clean modern wordmark
- Composition: centered wordmark, generous safe padding for Android adaptive icon mask, no extra objects
- Style: minimal, premium, modern utility app icon, crisp edges, high legibility at small sizes
- Output: 1024x1024 square master image; separate foreground wordmark layer and background layer if possible
- Avoid: people, mascots, cartoon characters, complex illustration, watermark, small unreadable text, extra slogans, fake UI, clutter

## Sky Blue

Create a 1024x1024 Android adaptive app icon. The only text is the exact word "Skip". Make "Skip" a modern white geometric wordmark with subtle depth, centered on a clear sky-blue background. Keep the style minimal, polished, and suitable for a utility app. Use generous padding for adaptive icon cropping. No other text, no objects, no characters, no watermark.

## Mint Green

Create a 1024x1024 Android adaptive app icon. The only text is the exact word "Skip". Make "Skip" a clean white rounded wordmark, centered on a fresh mint-green background. The look should feel calm, efficient, and premium. Keep edges crisp and readable at small sizes. Use generous adaptive icon safe padding. No other text, no illustration, no watermark.

## Violet Glow

Create a 1024x1024 Android adaptive app icon. The only text is the exact word "Skip". Make "Skip" a sleek white wordmark with a restrained violet glow accent, centered on a deep violet background. The icon should feel modern and refined, not flashy. Keep the word highly legible and inside adaptive icon safe bounds. No extra text, no characters, no watermark.

## Sunset Orange

Create a 1024x1024 Android adaptive app icon. The only text is the exact word "Skip". Make "Skip" a crisp white modern wordmark, centered on a warm sunset-orange background. Use a confident utility-app style with subtle dimensional polish. Keep the design simple and legible at launcher size. No extra objects, no slogan, no watermark.

## Cherry Pink

Create a 1024x1024 Android adaptive app icon. The only text is the exact word "Skip". Make "Skip" a refined white artistic wordmark, centered on a cherry-pink background. The design should be clean, energetic, and premium. Maintain generous padding for adaptive icon masks and strong small-size readability. No other text, no illustration, no watermark.

## Obsidian Gold

Create a 1024x1024 Android adaptive app icon. The only text is the exact word "Skip". Make "Skip" a warm metallic-gold modern wordmark, centered on a near-black obsidian background. The style should be premium, restrained, and suitable for a professional utility app. Keep the wordmark clear at small sizes and within Android adaptive icon safe padding. No other text, no objects, no watermark.
