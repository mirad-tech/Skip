# Skip Online Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to execute this plan. Do not start implementation until the user explicitly asks for it. This document is the release plan only.

## 0. 文档边界

本文件用于把当前功能验证版整理成真实上线前的发布计划。当前阶段只做阅读、分析和任务拆解，不修改现有业务代码，不新增功能实现。

上线方向是“本地自动点击辅助工具 / 开屏页面助手”。产品必须避免被描述成广告破解、广告屏蔽、绕过限制或规避第三方业务规则的工具。

## 1. 当前项目现状分析

### 1.1 项目主要模块

- 根工程是 Gradle Android 工程，当前主要业务模块为 `:app`。
- UI 层使用 Kotlin + Jetpack Compose，入口在 `app/src/main/java/com/example/skip/MainActivity.kt`，页面分布在 `ui/`、`ui/apps/`、`ui/logs/`、`ui/more/`、`ui/rules/`、`ui/settings/`、`ui/theme/` 等目录。
- 无障碍服务入口为 `app/src/main/java/com/example/skip/service/SkipAccessibilityService.kt`。
- 无障碍事件窗口追踪在 `app/src/main/java/com/example/skip/service/EventWindowTracker.kt`。
- 规则匹配和点击决策集中在 `app/src/main/java/com/example/skip/engine/`：
  - `RulePlanProvider.kt`
  - `NodeScanner.kt`
  - `RuleMatcher.kt`
  - `ScoreEvaluator.kt`
  - `CoordinateFallbackMatcher.kt`
  - `ClickExecutor.kt`
  - `SafetyGuard.kt`
- 数据持久化集中在 `app/src/main/java/com/example/skip/data/`：
  - `SettingsRepository.kt`
  - `RuleRepository.kt`
  - `RuleImportManager.kt`
  - `LogRepository.kt`
  - `StatsRepository.kt`
  - `InstalledAppRepository.kt`
- 数据模型集中在 `app/src/main/java/com/example/skip/model/`：
  - `SkipRule.kt`
  - `AppPolicy.kt`
  - `ClickLog.kt`
  - `HitStats.kt`
  - `CoordinateFallback.kt`
  - `RuleImportResult.kt`
- 单元测试当前集中在 `app/src/test/java/com/example/skip/`。
- 发布配置在 `app/build.gradle.kts`、`app/proguard-rules.pro`、根目录 `keystore.properties.example` 等文件。

### 1.2 已有功能

- 已有无障碍服务，可以监听窗口变化和窗口内容变化，并尝试匹配开屏页面上的跳过/关闭类控件。
- 已有主开关、基础设置、安全模式、提示开关等全局配置能力。
- 已有按应用策略能力，模型中已经存在 `AppPolicy`，规则规划也会读取应用策略。
- 已有规则系统，支持内置规则、用户规则、JSON 导入规则等来源。
- 已有规则导入、规则导出和规则管理相关 UI/数据能力。
- 已有坐标兜底模型和匹配器，支持在节点匹配失败时按配置的坐标执行兜底点击。
- 已有命中日志、规则事件日志、日志导出和统计聚合能力。
- 已有隐私、安全、权限、系统兼容等页面雏形。
- README 已经开始向“本地无障碍辅助工具”方向表述，并强调本地处理和无网络权限。

### 1.3 当前无障碍服务实现方式

当前 `SkipAccessibilityService` 的主要流程是：

1. 接收 `TYPE_WINDOW_STATE_CHANGED`、`TYPE_WINDOW_CONTENT_CHANGED`、`TYPE_WINDOWS_CHANGED` 等无障碍事件。
2. 通过 `EventWindowTracker`、事件包名和根节点包名综合判断当前前台包名。
3. 使用 `SafetyGuard` 跳过自身、系统保护包、权限/支付/登录等敏感页面。
4. 检查全局主开关和当前应用策略。
5. 通过 `RulePlanProvider` 合并应用策略、用户规则和内置规则，生成当前可用规则计划。
6. 使用 `NodeScanner`、`RuleMatcher`、`ScoreEvaluator` 扫描无障碍节点并计算命中结果。
7. 若没有节点命中，再尝试 `CoordinateFallbackMatcher`。
8. 命中后执行延迟稳定检查，防止窗口切换后误点。
9. 优先尝试 `ACTION_CLICK`，失败后使用节点中心点手势点击。
10. 坐标兜底命中时使用手势点击坐标。
11. 全流程写入 `ClickLog`，用于日志页和统计页。

当前服务已经有基础风险控制，但还需要把“高风险按钮保护”从通用敏感页面判断升级为明确、可测试、可审计的发布级策略。

### 1.4 当前规则系统

当前核心模型为 `SkipRule`，主要能力包括：

- 规则来源：`UserSimple`、`JsonFile`、`BuiltIn`、`Subscription`。
- 绑定应用：支持 `packageName`、`appName`、`activityName`。
- 匹配字段：`matchTexts`、`matchContentDescriptions`、`matchViewIds`。
- 匹配模式：`Contains`、`Exact`、`Regex`。
- 匹配区域：`RuleArea` 支持相对屏幕区域。
- 动作：当前主要为 `RuleAction.Click`。
- 风险控制字段：`priority`、`cooldownMs`、`validDurationMs`、`minScore`。
- 坐标兜底：`coordinateFallback`。

当前 `RuleRepository` 已有默认关键词和默认 ViewId 关键词，用于识别跳过/关闭类控件。规则计划会结合应用策略决定是否启用默认规则和自定义规则。

需要补齐的发布级能力：

- 高风险词策略必须独立、集中、可测试。
- 导入规则必须经过安全校验，尤其是坐标兜底规则。
- 自定义规则、内置规则、导入规则的权限边界和 UI 提示必须更清楚。
- 规则指南需要告诉用户如何创建低误触规则，而不是鼓励通用扫屏点击。

### 1.5 当前日志和统计能力

当前日志能力主要由 `ClickLog`、`LogRepository`、`ClickLogScreen` 等组成：

- 记录事件阶段、包名、应用名、规则名、命中文本、坐标、成功状态、失败原因等信息。
- 支持记录安全跳过事件。
- 支持日志列表、清理和 JSON 导出。
- 规则导入等事件也有日志记录能力。

当前统计能力主要由 `StatsRepository`、`HitStats`、`StatsScreen` 等组成：

- 可以按应用、规则、事件阶段和时间窗口统计命中情况。
- 当前统计主要来自本地日志，不依赖联网服务。

需要补齐的发布级能力：

- 高风险按钮被拦截时必须写入安全日志。
- 日志中必须有明确字段或原因值：`blocked_by_safety_policy`。
- 统计页需要能区分成功点击、未命中、冷却跳过、安全策略阻止等类型。
- 日志页需要避免记录过多用户隐私文本，只保留必要的脱敏命中信息。

### 1.6 当前权限情况

当前源码 Manifest 中核心权限情况：

- 无障碍服务声明了 `android.permission.BIND_ACCESSIBILITY_SERVICE`。
- 无障碍服务配置启用了：
  - `canRetrieveWindowContent="true"`
  - `canPerformGestures="true"`
  - `flagReportViewIds`
- 当前源码 Manifest 未看到普通网络权限，例如 `INTERNET`。
- 当前使用 Android 文档选择器导入/导出规则和日志时，应继续避免申请不必要的存储权限。

当前发布工程风险：

- 根目录存在 `keystore.properties` 和 `release.keystore`。上线前必须确认它们没有被提交到仓库，且不会出现在公开分发包或源码包中。
- `app/build.gradle.kts` 中 release 构建已经启用签名配置、R8 混淆和资源压缩，但仍需要做一次正式检查。

### 1.7 当前测试情况

当前已有单元测试目录 `app/src/test/java/com/example/skip/`，覆盖方向包括：

- 安全保护包和敏感页面判断。
- 默认规则时间窗口。
- 自定义规则有效期。
- 延迟点击校验。
- 规则计划选择。
- 坐标兜底匹配。
- 规则导入解析。
- 日志导出。
- 隐私脱敏。
- 统计聚合。
- 前台窗口追踪。

发布前测试缺口：

- 首次启动披露流程缺少明确验收用例。
- 用户主动同意后再引导开启无障碍缺少端到端验证。
- 高风险按钮完整词表缺少逐项测试。
- 坐标兜底限制缺少完整负例测试。
- 多机型、多 Android 版本、多 ROM 的人工测试矩阵需要补齐。
- release 构建、R8 后功能、签名流程、安装包验证需要补齐。

## 2. 真实上线目标

### 2.1 产品定位

产品定位改为：

- 本地自动点击辅助工具。
- 开屏页面助手。
- 帮助用户减少重复、明确、低风险的手动点击。
- 只在用户主动开启无障碍服务并配置规则后工作。

产品文案必须避免：

- 广告破解。
- 广告屏蔽。
- 绕过广告。
- 跳过限制。
- 破解第三方应用机制。
- 承诺对所有应用生效。

### 2.2 合规边界

- 不复制、不逆向、不照搬李跳跳或其他成熟产品代码。
- 可以参考成熟产品的用户体验完整度、风险提示方式、规则管理体验和发布前质量要求。
- 不读取、上传、出售、分析用户屏幕内容和个人数据。
- 默认本地处理。
- 默认不联网。
- 不引入远程规则订阅，除非后续单独设计隐私、来源审核、用户确认和关闭能力。
- 不对支付、授权、登录、隐私同意等场景执行自动点击。

### 2.3 发布质量目标

- 低误触：默认规则保守，坐标兜底默认关闭，高风险按钮默认拦截。
- 可解释：用户可以看到为什么点击、为什么未点击、命中了哪条规则。
- 可关闭：用户可以按应用关闭、按规则关闭、全局关闭。
- 可维护：规则、日志、统计、文档和测试有清晰边界。
- 可审核：隐私政策、权限说明、发布说明、测试矩阵完整。

## 3. 必须完成的功能改造

### 3.1 首次启动明显披露页

必须新增首次启动披露流程，首次打开 App 时展示：

- 产品是本地自动点击辅助工具 / 开屏页面助手。
- 需要无障碍权限才能识别屏幕控件并执行点击。
- 不保证对所有应用生效。
- 不用于支付、授权、登录、隐私同意等高风险场景。
- 默认不联网，不上传屏幕内容和个人数据。
- 用户可以随时关闭主开关、应用开关、规则开关或无障碍服务。

验收要求：

- 未同意披露前，不引导开启无障碍权限。
- 用户拒绝后仍可浏览隐私政策、权限说明和设置，但服务不应被引导开启。
- 同意状态本地保存，并提供后续查看入口。

### 3.2 无障碍权限用途说明页

必须新增或强化无障碍权限用途说明页，清楚解释：

- 读取窗口内容：仅用于在本机识别用户配置的跳过/关闭类按钮。
- 执行手势：仅用于点击明确命中的低风险控件。
- 报告 ViewId：用于提高规则匹配准确性。
- 不上传屏幕内容。
- 不记录完整页面内容。
- 不用于高风险按钮自动点击。

### 3.3 用户主动同意后再引导开启无障碍

流程必须调整为：

1. 首次披露页。
2. 用户主动勾选或点击同意。
3. 展示无障碍权限用途说明。
4. 用户再次点击“去开启”。
5. 跳转系统无障碍设置。

不允许：

- 首屏直接弹出系统设置引导。
- 用模糊文案诱导用户授权。
- 把拒绝路径隐藏得很深。

### 3.4 高风险点击保护

必须实现集中式高风险点击保护策略：

- 对所有规则来源生效。
- 对内置规则、用户规则、导入规则、坐标兜底都生效。
- 命中高风险内容时，不执行点击。
- 只写安全日志。
- 日志原因标记为 `blocked_by_safety_policy`。

当前 `SafetyGuard` 已有部分敏感关键词和保护包逻辑，但发布前需要补齐完整词表、测试和日志原因。

### 3.5 坐标兜底默认关闭

坐标兜底必须改为高风险高级功能：

- 全局默认关闭。
- 每条规则默认关闭。
- 导入规则中的坐标兜底默认需要安全校验和用户确认。
- UI 中必须有明确风险说明。
- 坐标兜底不参与默认规则。

### 3.6 按应用开关

必须保证用户可以：

- 按应用启用或停用自动辅助。
- 按应用启用或停用默认规则。
- 按应用启用或停用自定义规则。
- 按应用查看命中日志和统计。
- 快速把误触应用加入停用状态。

### 3.7 规则管理

规则管理必须支持：

- 查看规则来源。
- 查看绑定应用。
- 查看匹配文本、描述、ViewId、区域、时间窗口、冷却时间。
- 单条规则启停。
- 删除用户规则。
- 校验导入规则。
- 标记高风险或不合规规则。
- 禁止创建没有包名约束的坐标兜底规则。

### 3.8 导入导出

导入导出必须满足：

- 导入前预览变更。
- 导入时校验包名、匹配条件、坐标兜底、安全词。
- 导入后写规则事件日志。
- 导出时不包含用户隐私日志。
- 导出规则文件格式写入 `RULES_GUIDE.md` 和 `sample_rules.json`。

### 3.9 命中日志

命中日志必须支持：

- 成功点击。
- 冷却跳过。
- 安全策略阻止。
- 规则未通过校验。
- 坐标兜底命中但被安全策略阻止。
- 失败原因清晰展示。

高风险拦截日志必须包含：

- 包名。
- 应用名。
- 规则来源。
- 规则名或规则 ID。
- 命中字段类型。
- 脱敏后的命中文本。
- `blocked_by_safety_policy`。

### 3.10 统计页

统计页必须支持：

- 总命中次数。
- 成功点击次数。
- 安全阻止次数。
- 按应用统计。
- 按规则统计。
- 按时间窗口统计。
- 坐标兜底命中次数。
- 最近 24 小时 / 7 天统计。

### 3.11 隐私政策页面

隐私政策页面必须说明：

- 数据本地处理。
- 不上传屏幕内容。
- 不收集账号、密码、验证码、支付信息。
- 日志仅本地保存。
- 规则导入导出由用户主动触发。
- 无网络权限时的隐私边界。
- 如果未来新增联网能力，必须更新隐私政策并获得用户确认。

### 3.12 权限说明页面

权限说明页面必须列出：

- 无障碍权限。
- 文档选择器导入导出能力。
- 是否存在通知权限。
- 是否存在网络权限。
- 是否存在存储权限。
- 每个权限的用途、是否必需、如何关闭。

### 3.13 README 更新

README 必须更新为上线口径：

- 本地自动点击辅助工具 / 开屏页面助手。
- 不宣传广告破解、广告屏蔽、绕过。
- 明确无障碍权限用途。
- 明确隐私承诺。
- 明确高风险场景不会自动点击。
- 明确坐标兜底默认关闭且不建议普通用户开启。
- 给出构建、测试、发布文档入口。

### 3.14 发布说明文档

必须新增发布说明文档，记录：

- 版本号。
- 发布日期。
- 新增功能。
- 安全改造。
- 已知限制。
- 测试结果摘要。
- 升级注意事项。

## 4. 高风险点击保护规则

### 4.1 默认禁止自动点击的内容

默认禁止自动点击包含以下含义的按钮、文本、描述或相关节点：

- 同意
- 授权
- 允许
- 支付
- 购买
- 确认支付
- 登录
- 注册
- 隐私政策
- 用户协议
- 安装
- 删除
- 卸载
- 转账
- 发送
- 提交

匹配范围：

- 节点文本。
- 节点 contentDescription。
- 相关父子节点文本。
- 规则匹配文本。
- 导入规则中的描述字段。
- 坐标兜底锚点文本。

### 4.2 命中后的行为

命中这些内容时必须：

- 不执行 `ACTION_CLICK`。
- 不执行手势点击。
- 不执行坐标兜底点击。
- 写入安全日志。
- 日志标记为 `blocked_by_safety_policy`。
- 不把该事件计入成功点击。
- 在统计页计入安全阻止次数。

### 4.3 实现建议

建议新增或重构为集中策略：

- `HighRiskClickPolicy`：负责词表、匹配和原因返回。
- `SafetyGuard`：继续负责保护包、保护页面、自身包和系统级防护。
- `SkipAccessibilityService`：在真正点击前统一调用高风险策略。
- `RuleImportManager`：导入规则时提前识别高风险规则。
- `LogRepository` / `ClickLog`：支持明确的 `blocked_by_safety_policy` 原因。

### 4.4 测试要求

必须为每个高风险词添加测试：

- 节点文本命中后不点击。
- contentDescription 命中后不点击。
- 导入规则包含高风险词时被标记或拒绝。
- 坐标兜底锚点包含高风险词时不点击。
- 日志原因等于 `blocked_by_safety_policy`。

## 5. 坐标兜底限制

### 5.1 默认状态

坐标兜底必须：

- 默认关闭。
- 不出现在默认规则中。
- 不自动从内置规则启用。
- 不因导入规则而静默启用。

### 5.2 允许使用的前提

坐标兜底只能用于用户手动创建或主动导入的规则，并且必须同时满足：

- 必须绑定包名。
- 必须限制应用启动后的时间窗口。
- 必须有锚点规则。
- 必须有点击冷却时间。
- 必须有明确规则名称和来源。
- 必须通过高风险点击保护策略。
- 必须在 UI 中显示风险提示。

### 5.3 禁止场景

坐标兜底不允许用于：

- 支付。
- 授权。
- 登录。
- 注册。
- 隐私同意。
- 用户协议同意。
- 安装。
- 卸载。
- 删除。
- 转账。
- 发送。
- 提交。

### 5.4 验收规则

- 未绑定包名的坐标兜底规则无法保存。
- 没有锚点规则的坐标兜底规则无法保存。
- 没有冷却时间的坐标兜底规则无法保存。
- 超出启动时间窗口不执行坐标点击。
- 命中高风险词不执行坐标点击。
- 导入坐标兜底规则必须经过预览和确认。

## 6. 发布工程要求

### 6.1 release buildType 检查

当前 `app/build.gradle.kts` 中已存在 `release` buildType，并启用了：

- `isMinifyEnabled = true`
- `isShrinkResources = true`
- `proguard-android-optimize.txt`
- `proguard-rules.pro`
- release 签名配置

发布前必须检查：

- release 构建是否可以成功执行。
- R8 后无障碍服务、Compose 页面、规则导入导出是否正常。
- 版本号和版本名是否符合发布计划。
- release 构建不会依赖本地绝对路径。

### 6.2 ProGuard/R8 配置检查

必须检查：

- 无障碍服务类不会被错误混淆或移除。
- Compose 页面正常。
- JSON 序列化/反序列化所需模型不会被破坏。
- 导入导出规则字段兼容。
- release 安装包运行后无崩溃。

### 6.3 签名文件和密钥

必须做到：

- 不把签名文件写入仓库。
- 不把签名密码写入仓库。
- 不把密钥写入仓库。
- `keystore.properties` 只能作为本地文件。
- `release.keystore` 只能作为本地文件。
- 仓库中只保留 `keystore.properties.example`。
- 发布前检查 `.gitignore` 是否覆盖真实签名文件和密码文件。

当前根目录存在 `keystore.properties` 和 `release.keystore`，必须在发布工程阶段确认它们未被 Git 跟踪。如已被跟踪，需单独执行密钥轮换和历史风险处理。

### 6.4 必须新增的发布文档和样例

后续实现阶段必须新增：

- `RELEASE_GUIDE.md`
- `COMPLIANCE_CHECKLIST.md`
- `RELEASE_TEST_MATRIX.md`
- `RULES_GUIDE.md`
- `sample_rules.json`

各文件内容要求：

- `RELEASE_GUIDE.md`：构建、签名、版本号、发布前检查、回滚流程。
- `COMPLIANCE_CHECKLIST.md`：权限、隐私、文案、高风险点击、坐标兜底、日志脱敏检查。
- `RELEASE_TEST_MATRIX.md`：Android 版本、厂商 ROM、无障碍开关、真实 App 场景、导入导出、release 构建。
- `RULES_GUIDE.md`：规则字段说明、低风险规则建议、坐标兜底限制、导入格式。
- `sample_rules.json`：只包含低风险、绑定包名、有限时间窗口、无高风险词的示例规则。

## 7. 测试要求

每个阶段完成后必须运行：

```powershell
.\gradlew.bat :app:testDebugUnitTest
.\gradlew.bat :app:assembleDebug
git diff --check
```

如果支持 release 构建，也尝试：

```powershell
.\gradlew.bat :app:assembleRelease
```

### 7.1 阶段测试拆分

阶段 A：合规披露和权限说明

- 单元测试：首次同意状态、拒绝状态、引导状态。
- 手工测试：首次启动、拒绝、同意、二次查看隐私和权限说明。
- 构建测试：debug 单测和 assembleDebug。

阶段 B：高风险点击保护

- 单元测试：逐项高风险词。
- 单元测试：文本、描述、父子节点、导入规则、坐标锚点。
- 手工测试：模拟包含高风险按钮的页面，不发生点击。
- 日志测试：出现 `blocked_by_safety_policy`。

阶段 C：坐标兜底限制

- 单元测试：无包名、无锚点、无冷却、超时、高风险词全部失败。
- 手工测试：普通低风险坐标兜底只在绑定应用和时间窗口内生效。
- 导入测试：风险规则被拒绝或要求确认。

阶段 D：规则管理、导入导出、日志统计

- 单元测试：规则增删改、导入导出格式、日志统计分类。
- 手工测试：按应用开关、按规则开关、日志筛选、统计页。
- 文件测试：导出的 JSON 不含敏感日志。

阶段 E：发布工程和文档

- 检查 release buildType。
- 检查 ProGuard/R8。
- 检查签名文件和密钥不入仓库。
- 检查 README 和所有发布文档。
- 运行 debug/release 构建。

### 7.2 人工测试矩阵最低要求

发布前至少覆盖：

- Android 9、10、11、12、13、14、15、16 中可获得的真实设备或模拟器。
- 原生 Android、MIUI/HyperOS、ColorOS、OriginOS、HarmonyOS 兼容 Android 场景中的可获得设备。
- 开启无障碍、关闭无障碍、系统回收服务后的恢复。
- 应用冷启动、热启动、后台返回。
- 低风险开屏按钮。
- 高风险按钮页面。
- 无按钮页面。
- 坐标兜底规则。
- 导入规则。
- 导出规则。
- 日志清理。
- release 包安装和升级。

## 8. 完成标准

真实上线前必须全部满足：

- debug 构建成功。
- 单元测试通过。
- 如 release 构建已配置，release 构建成功。
- 无无关敏感权限。
- 首次启动合规披露完整。
- 用户主动同意后才引导开启无障碍。
- 无障碍用途说明清楚。
- 高风险按钮不会被点击。
- 命中高风险按钮时只写安全日志。
- 高风险安全日志标记为 `blocked_by_safety_policy`。
- 坐标兜底默认关闭。
- 坐标兜底受包名、时间窗口、锚点、冷却时间和安全策略严格限制。
- 按应用开关可用。
- 规则管理可用。
- 导入导出可用且有安全校验。
- 命中日志可用。
- 统计页可用。
- 隐私政策页面完整。
- 权限说明页面完整。
- README 更新完成。
- `RELEASE_GUIDE.md` 完整。
- `COMPLIANCE_CHECKLIST.md` 完整。
- `RELEASE_TEST_MATRIX.md` 完整。
- `RULES_GUIDE.md` 完整。
- `sample_rules.json` 完整。
- 签名文件、密码、密钥没有写入仓库。
- 最后输出改动总结和验证结果。

## 9. 建议执行顺序

### Phase 1：合规入口和权限说明

- 新增首次启动披露页。
- 新增或强化无障碍权限用途说明页。
- 增加用户主动同意状态。
- 调整无障碍引导流程。
- 增加相关单元测试和手工测试记录。

### Phase 2：安全策略

- 建立高风险点击保护策略。
- 补齐高风险词表。
- 将策略接入节点点击、手势点击和坐标兜底。
- 日志写入 `blocked_by_safety_policy`。
- 增加逐词测试。

### Phase 3：坐标兜底收紧

- 坐标兜底默认关闭。
- 保存规则时校验包名、时间窗口、锚点和冷却时间。
- 导入规则时校验坐标兜底。
- UI 增加风险提示。
- 增加负例测试。

### Phase 4：规则、日志、统计完善

- 完善按应用开关体验。
- 完善规则管理页。
- 完善导入导出预览和校验。
- 完善命中日志分类。
- 完善统计页维度。

### Phase 5：发布文档和工程检查

- 更新 README。
- 新增 `RELEASE_GUIDE.md`。
- 新增 `COMPLIANCE_CHECKLIST.md`。
- 新增 `RELEASE_TEST_MATRIX.md`。
- 新增 `RULES_GUIDE.md`。
- 新增 `sample_rules.json`。
- 检查 release buildType、R8、签名文件和密钥。

### Phase 6：发布前总验收

- 运行 debug 单元测试。
- 运行 debug 构建。
- 运行 diff 空白检查。
- 尝试 release 构建。
- 按测试矩阵做人工测试。
- 输出最终改动总结和验证结果。

## 10. 当前阶段不执行的事项

本次只创建 `ONLINE_RELEASE_PLAN.md`，不执行以下事项：

- 不修改 Kotlin 源码。
- 不修改 Compose UI。
- 不修改 Manifest。
- 不修改 Gradle 配置。
- 不新增测试。
- 不新增发布文档之外的文件。
- 不运行真实发布构建。
- 不处理签名文件。

