# Skip 合规检查表

本检查表用于每次发布前确认产品定位、权限、隐私、安全策略和发布工程满足真实上线要求。

本轮源码、文档和自动化命令审计日期：2026-05-14。人工真机验证仍需按 `RELEASE_TEST_MATRIX.md` 完成。

## 产品定位

- [x] 应用名称、README、页面文案使用“本地自动点击辅助工具 / 开屏页面助手”口径。
- [x] 不宣传为广告破解、广告屏蔽、绕过工具。
- [x] 不承诺对所有第三方应用生效。
- [x] 不复制、不逆向、不照搬李跳跳或其他同类产品代码。
- [x] 不提交或分发带有李跳跳对照、逆向、照搬暗示的历史计划文件。
- [x] 规则指南只描述低风险重复点击辅助，不鼓励通用扫屏点击。

## 数据和隐私

- [x] 默认本地处理。
- [x] 不上传屏幕内容。
- [x] 不上传规则、日志、统计或个人数据。
- [x] 日志只保存在本机。
- [x] 日志字段做最小化记录和脱敏。
- [x] 导入、导出必须由用户主动触发。
- [x] 隐私说明页面写明未来如新增联网能力，必须更新隐私政策并重新获得用户确认。

## 权限

- [x] Manifest 无 `INTERNET` 权限。
- [x] Manifest 无定位权限。
- [x] Manifest 无通讯录权限。
- [x] Manifest 无相机权限。
- [x] Manifest 无麦克风权限。
- [x] Manifest 无短信权限。
- [x] Manifest 无外部存储读写权限。
- [x] 仅声明无障碍服务所需的 `android.permission.BIND_ACCESSIBILITY_SERVICE`。
- [x] 权限说明页解释无障碍读取窗口内容、执行手势、报告 View ID 的用途。

## SDK 和联网

- [x] 未接入广告 SDK。
- [x] 未接入统计 SDK。
- [x] 未接入联网 SDK。
- [x] 未新增远程规则订阅。
- [x] 如后续新增联网能力，必须重新评估隐私政策、权限说明和用户同意流程。

## 首次启动和授权

- [x] 首次启动展示明显披露页。
- [x] 用户未主动同意前，不引导开启无障碍。
- [x] 用户同意后先展示无障碍用途说明。
- [x] 用户再次点击“去系统设置开启”后才打开系统无障碍设置。
- [x] 用户拒绝后仍可查看隐私说明和权限说明。

## 高风险点击保护

- [x] 默认禁止自动点击：同意、授权、允许、支付、购买、确认支付、登录、注册、隐私政策、用户协议、安装、删除、卸载、转账、发送、提交。
- [x] 命中高风险内容时不执行 `ACTION_CLICK`。
- [x] 命中高风险内容时不执行手势点击。
- [x] 命中高风险内容时不执行坐标兜底。
- [x] 命中高风险内容时只写安全日志。
- [x] 安全日志原因标记为 `blocked_by_safety_policy`。
- [x] 单元测试逐项覆盖高风险词。

## 坐标兜底

- [x] 默认关闭。
- [x] 不用于内置规则。
- [x] 只能用于用户手动创建或主动导入的规则。
- [x] 必须绑定包名。
- [x] 必须限制启动后的时间窗口。
- [x] 必须有锚点规则。
- [x] 必须有点击冷却时间。
- [x] 必须通过高风险点击保护。
- [x] 不允许用于支付、授权、登录、隐私同意等场景。

## 发布工程

- [x] release buildType 存在。
- [x] release 构建启用 R8 / ProGuard。
- [x] release 构建启用资源压缩。
- [x] `keystore.properties` 不被 Git 跟踪。
- [x] `release.keystore` 不被 Git 跟踪。
- [x] 签名文件、密码、密钥未写入仓库。
- [x] `.gitignore` 覆盖真实签名文件和本地配置。

## 发布前命令

```powershell
.\gradlew.bat :app:testDebugUnitTest
.\gradlew.bat :app:assembleDebug
git diff --check
.\gradlew.bat :app:assembleRelease
```

本轮结果：2026-05-14 四项均通过；`git diff --check` 仅输出 CRLF 工作区提示，无空白错误。
