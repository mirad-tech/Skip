# 贡献指南

感谢你帮助改进 Skip。请在开始前阅读本项目的功能边界与安全要求，并使每个改动保持单一、可审查、可验证。

## 报告问题

- 普通故障请使用 [问题报告模板](https://github.com/mirad-tech/Skip/issues/new/choose)。
- 功能建议请说明真实使用场景、预期结果以及对自动化安全边界的影响。
- 提交日志、截图或诊断包前，请删除账号、验证码、支付信息、不希望公开的应用名称与包名。
- 安全或隐私漏洞不要发布在公开 Issue，请按 [SECURITY.md](SECURITY.md) 私下报告。

## 开发环境

- Android Studio 与项目 Gradle Wrapper。
- Android SDK 36.1，最低运行版本为 Android 9（API 28）。
- 使用仓库自带的 `gradlew` / `gradlew.bat`，不要提交本地 SDK 路径、签名文件或密码。

Windows 上的最小验证：

```powershell
.\gradlew.bat :app:testDebugUnitTest --rerun-tasks
.\gradlew.bat :app:assembleDebug
git diff --check
```

Release 构建使用维护者的本地签名配置。外部贡献者不需要提供或索取任何真实签名材料。

## 安全边界

代码改动不得：

- 扩大到支付、授权、登录、注册、隐私同意、安装、删除、转账、发送或提交等高风险操作。
- 绕过首次披露、用户主动授权、应用开关、规则开关、冷却、时间窗或高风险点击策略。
- 增加与功能无关的敏感权限、远程上传、追踪、广告 SDK、Root、Hook 或注入能力。
- 将真实签名文件、密码、设备诊断数据或其他秘密提交到仓库。

如果变更无法证明上述边界保持不变，请先在 Issue 中说明设计和风险，不要直接扩大实现。

## Pull Request

- 一个 PR 只解决一组相关问题，避免混入格式化、重构或无关文档改动。
- 说明问题、根因、实际改动、安全/权限影响和已执行的验证。
- 行为改动应增加能够覆盖该回归的测试；无法自动验证时，明确列出未验证项。
- 修改用户可见行为、规则格式或发布流程时，同步更新对应文档。
